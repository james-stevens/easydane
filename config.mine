#
# Fill out these fields
#
# where
#
# C = Country
# ST = State, Province or County
# L = Location, City or Office
# O = Organization Name
# OU = Organizational Unit Name
#
# server_name = full name of your website / server (e.g "www.domain.txt")

C="GB"
ST="Berks"
L="Windsor"
O="JRCS Ltd"
OU="R-and-D"

server_name="sane.txt"
